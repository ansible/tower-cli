sudo: false
language: python
python:
    - "2.6"
    - "2.7"
    - "3.3"
    - "3.4"
    - "3.5"
    - "pypy"
env:
    - TOXENV=flake8
install:
    - pip install flake8
    - pip install tox coveralls --use-mirrors
script:
    - tox
after_success:
    coveralls
